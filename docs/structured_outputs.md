# Structured outputs

Use a Pydantic model as the boundary between model text and runtime behavior. Parse JSON, validate types and constraints, and retry only for recoverable formatting failures. Never execute a tool from unvalidated model text. Structured output makes routing, tool calls, tests, and observability dependable.
