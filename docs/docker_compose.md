# Docker Compose

Compose documents local services, environment, ports, and health relationships. Run `docker compose up --build` and stop with `docker compose down`. The root file exposes both comparison apps as profiles so learners start only what they need. Keep stateful or heavy optional services commented and explain why they are optional.
