# Jupyter

Run `uv sync --group dev`, register the kernel shown in [uv.md](uv.md), then run `uv run jupyter lab`. Select **Agentic AI Engineering**. Restart the kernel after dependency changes. Clear outputs before sharing notebooks to avoid leaking keys, user data, or costly accidental reruns.
