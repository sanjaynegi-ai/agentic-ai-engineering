# Cost control

Enforce maximum input length, maximum output tokens, maximum steps, a model allowlist, per-session quotas, rate limits, caching, and provider spend limits. Protect API keys server-side. Estimate cost before expensive fan-out, record usage after each call, and stop when a budget is exceeded. Smaller models are sensible defaults for exercises; quality-sensitive tasks should be evaluated before switching.
