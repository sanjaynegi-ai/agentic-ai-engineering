# ECS Fargate deployment

The executable deployment configuration lives in `infra/terraform/ecr` and `infra/terraform/ecs`. Dispatch `.github/workflows/deploy-ecs-manual.yml`; do not edit generated task definitions by hand. See `docs/project_to_aws.md`.
